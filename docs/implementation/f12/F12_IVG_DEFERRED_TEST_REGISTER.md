# F12 — Integrated Verification Gate (IVG) Deferred Test Register

Reconciles and continues `F10_IVG_DEFERRED_TEST_REGISTER.md` (task §61). No prior entry is silently removed — each is either carried forward unchanged, extended, or explicitly superseded with a reason. New F12-specific deferrals are appended.

**Note on lineage**: F11-A through F11-C4 and the F11 Integrated Certification did not create their own IVG register (not required by those phases' own task specs). This register therefore carries forward directly from F10's, and additionally extends each still-open item's scope to cover every migration/route added since (F11-B through F12's own), rather than inventing a separate near-duplicate entry per intervening phase.

## Carried forward from F7–F10

| ID | Phase origin | Requirement | Status | Notes |
|---|---|---|---|---|
| IVG-F7-01 | F7 | Remote Preview database's actual migration state should be confirmed before pilot use | **STILL OPEN, EXTENDED** | Now also covers every migration added since F9 (F11-B, F11-C1, F11-C2, F11-C3, F11-C4, F12's own `institution_analytics_policy_versions`) — none of these has been applied to or verified against a real Preview database in this environment. |
| IVG-F8-01 | F8 | AI_REAL_PROVIDER certification | **SUPERSEDED by IVG-F9-01** (unchanged) | F11/F12 introduce zero new AI call sites (F12 structurally guarded against any, task §34) — nothing new to consolidate here. |
| IVG-F8-02 | F8 | Remote authenticated E2E matrix against live Preview | **STILL OPEN, EXTENDED** | Now also covers F11's Teacher/Student intervention routes and F12's 11 new `/api/institutions/[id]/intelligence/*` routes, all of which share existing, already-certified authorization primitives (`isOwner`/`canTeacherAccessLearner`/`canAccessInstitution`/`canAccessClass`) — no new authorization mechanism was introduced that would need its own separate remote-E2E deferral. |
| IVG-F8-03 | F8 | Live Preview admin accept-path exercised as authenticated ADMIN requests | **STILL OPEN** | Unaffected by F11/F12 — neither adds admin-approval routes. |
| IVG-F9-01 | F9 | AI_REAL_PROVIDER certification for simulation item generation/grading | **STILL OPEN** | Unaffected by F11/F12. |
| IVG-F9-02 | F9 | Confirm F9's own migration applies cleanly to the real Preview database | **STILL OPEN** | Folded into the extended `IVG-F7-01` above going forward — not duplicated as a separate line. |
| IVG-F9-03 | F9 | Idempotency conflict-recovery under real network-level retry | **STILL OPEN** | Unaffected by F11/F12; F12 introduces no new idempotency mechanism (it is read-only). |
| IVG-F10-01 | F10 | Live Preview Parent relationship lifecycle via two real authenticated Clerk sessions | **STILL OPEN** | Unaffected by F12 (F12 grants Parents zero new access, real-Postgres proven, Case U). |
| IVG-F10-02 | F10 | Remote authenticated E2E for the Parent read-model surface | **STILL OPEN** | Unaffected by F12. |

## New in F12

| ID | Requirement | Why deferred | Dependency | Risk if not executed | Planned IVG execution | Pass criteria |
|---|---|---|---|---|---|---|
| IVG-F12-01 | Remote authenticated E2E for the 11 new `/api/institutions/[id]/intelligence/*` routes: a real Institution Admin session against Institution A, a real actor with NO membership, and a real actor who is an admin of a DIFFERENT institution B, each hitting the live routes | No mechanism to authenticate as multiple distinct real Clerk users against Preview from this environment (same limitation as every prior remote-E2E deferral) | Real Preview-environment Clerk test accounts with real `institution_memberships` rows in two distinct institutions | Low — the full authorization matrix (38 assertions including cross-institution isolation) is proven for real against real Postgres at the service layer; this item would add proof at the HTTP+session+route-wrapper layer only | At IVG time, drive the three-actor matrix against live Preview, asserting the same ALLOW/DENY outcomes the local certification already proved | Every case matches the locally-certified outcome exactly, including the 403/404 status codes the route wrappers translate errors into |
| IVG-F12-02 | Confirm F12's own migration (`institution_analytics_policy_versions`) applies cleanly to the real Preview database, and that it remains seeded with ZERO rows there until a real product decision inserts one | Remote migration policy: never apply to remote Preview DB until isolation from Production is proven (unchanged standing policy, task §39/§60) | A verified-isolated staging/Preview database, or an explicit isolation proof | Low — the local idempotent-reapplication proof is real; this adds only remote-environment confirmation | At IVG time, once isolation is proven, apply the migration and confirm zero rows exist, matching the local cert script's own verification query | Table/index present; zero rows; a subsequent `getActiveAnalyticsPolicy()` call still throws `NoActiveAnalyticsPolicyError` until a real policy is inserted |
| IVG-F12-03 | Large-scale (thousands-of-learners) performance characterization, beyond this phase's 50-learner local synthetic baseline | No representative large dataset or load-testing tool available in this environment | A staging environment with a realistically-sized synthetic or anonymized dataset | Medium — the query SHAPE is set-based and reviewed as N+1-free, but true latency at scale (index effectiveness, connection pool contention, network hop cost) has not been measured | At IVG time (or closer to pilot), run the same `f12-performance-baseline-runner.ts` methodology against a larger dataset, ideally in a Preview-like environment | p95 remains within an agreed SLA (not yet defined — an open product decision, not invented here) |
| IVG-F12-04 | Institution-level small-cohort policy: a real, approved product decision for `minimumCohortSize` (task §27's MIN_COHORT_POLICY: OPEN_DECISION) | No approved product/privacy decision exists yet (the discovery branch's "10" was not independently re-confirmed in this session) | A product/privacy owner decision | Medium — without a real ACTIVE policy row, every cohort-suppression-dependent aggregate fails closed (safe, but unusable) in Production until resolved | Before Preview/pilot rollout, insert a real `ACTIVE` `institution_analytics_policy_versions` row reflecting an actually-approved threshold | A documented, approved decision exists and is reflected as a real, versioned policy row |

## Discipline

No entry above is silently upgraded to PASS anywhere in this phase's reports. `F12_QA_REPORT.md`'s test-layer matrix marks `REMOTE_AUTHENTICATED_E2E`/`REMOTE_PREVIEW_SMOKE`-adjacent items as `DEFERRED`/`IVG_DEFERRED`, never `PASS`.
