# F15 — Residual Risk Register

## F15-C1 update (2026-09-20): R2 RESOLVED, R1 unchanged (still OPEN — sole remaining hard gate)

R2 (Preview Clerk misconfiguration) is closed — the operator corrected the Preview-scope Clerk keys and this agent independently re-verified `/sign-in` live on three separate deployments since the fix, not just once. R1 (credential rotation) is **unaffected by this and remains `OPEN`, `OPERATOR_ACTION_REQUIRED`, and the sole remaining hard Pilot/Production gate** — Pilot readiness will not be declared until rotation of all 5 credentials is completed and verified as a coordinated update (see the credential-scope audit and rotation-order runbook in F15_SECRET_AND_ENVIRONMENT_HARDENING.md's own F15-C1 addendum). All three local temp files that briefly held real DB connection strings during the Neon migration trial (`/private/tmp/studyus-f15-migration-trial-20260919/{clone-url,preview-url,preview.env}`) were securely deleted this sub-phase without their contents ever being read or documented by this agent.

## FASE 0 FREEZE (2026-09-21): R1 is no longer the sole hard gate — R13 added below

A manual exam-taking attempt performed 2026-09-21 (the part of R3's "operator-assisted login session" that covers the Student exam-taking leg) found the seeded Pilot exam catalog does not produce a usable exam: `MINI_MOCK`, question 1 of 1, `CONCEPT_NOT_MATCHED`, only action "Omitir esta parte." This is registered below as **R13**, a new hard Pilot gate independent of R1. "R1... sole remaining hard gate" above is preserved as the honest record of belief at the time (2026-09-20) and is not deleted, but must not be cited as current. See `docs/implementation/f15/F15_PHASE0_ACCEPTANCE_FREEZE.md` for the full reconciliation, including the newly-frozen `NOT_CERTIFIED` state of roles/student-license/parent/teacher/institution/multi-role (none of which is a new *risk* row here, since none was ever claimed done — they are frozen acceptance states, tracked in the Fase 0 document, not additional register rows).

| ID | Description | Evidence | Severity | Probability | Impact | Mitigation | Owner/domain | Pilot blocking? | Production blocking? |
|---|---|---|---|---|---|---|---|---|---|
| R1 | A real, credential-bearing `.env.local` (Clerk/DB/AI-provider secrets) remains un-rotated/unverified in a sibling scratchpad worktree | F14/F15/F15-C1 direct inspection; rotation blocked by this environment's own safety controls | **Critical** | Unknown (cannot be determined without operator access) | If live, an unrelated process on this host could connect to real external systems | Never committed to git (verified); operator must rotate/verify via each provider's own console | Local dev environment hygiene | **YES — hard gate** *(no longer described as "sole remaining" — see R13 and FASE 0 FREEZE note above)* | **YES — hard gate** |
| R2 | ~~Preview's Clerk configuration resolves to an unrelated application ("PMO OWN"), not StudyUS~~ **RESOLVED (F15-C1, 2026-09-20)** | **Historical finding** (preserved, not deleted): directly observed on the original Preview deployment (`dpl_B2xeHtPKgeMcZyfPoDQGDFxnqax3`) that `/sign-in` rendered "Sign in to PMO OWN" — a real, confirmed misconfiguration at the time. **Why it no longer applies**: the operator corrected the Preview-scope `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` in the Vercel dashboard (Preview scope only; Production keys never touched, independently confirmed via `vercel env ls`'s own separate-value listing). This agent then re-verified `/sign-in` live and independently on **three separate subsequent deployments** (`dpl_3X1LBcwU1PxNahLKzWaeEnGeoLdq`, `dpl_qz9b6nwoUpUEx8pYxpzs6oPxHR7h`, `dpl_Ets57qwU4z1MLtwzYrbLeFBYd1Uj`), each rendering "Sign in to StudyOS_App" with the Development-mode badge intact — i.e. the correct application, the correct (non-live) instance, confirmed stable across every code change since the fix, not a one-off observation | ~~Critical~~ Closed | N/A | None (closed) | Operator corrected the Preview environment's Clerk keys in the Vercel dashboard (Preview scope only, confirmed no Production change) | Deployment configuration | **NO — closed** | No |
| R3 | Full authenticated E2E (Student/Teacher/Parent/Institution/multi-role/negative-auth) unexecuted | F15_AUTHENTICATED_E2E_REPORT.md | High | Certain (disclosed) | Real user-facing defects in live, authenticated flows remain unknown | No longer blocked by Clerk (R2 resolved); structural/real-Postgres regression evidence exists as a partial substitute; still needs an operator-assisted login session before live E2E can run. **[FASE 0 FREEZE, 2026-09-21]**: the Student exam-taking leg of this E2E was executed and found a real defect (see R13) — "unexecuted" no longer describes that leg; the other legs (Teacher/Parent/Institution/multi-role/negative-auth) remain genuinely unexecuted | All workspaces | Yes — for full confidence, though structural evidence is real | Yes |
| R4 | Timed exam enforcement (`TRAINING_TIMED`/`OFFICIAL_SIMULATION_TIMED`) is accepted at attempt-start but not enforced or displayed by the new `ItemRunner` | F15_EXAM_TAKING_EXPERIENCE.md, disclosed scope limit | Medium | Certain (disclosed) | A Student could take a "timed" exam with no actual time pressure or countdown UI | Only `UNTIMED` attempts are the phase's own tested path; timed modes should not be offered to real pilot users until built | Exam-Taking UX | Yes, if the pilot intends to use timed modes | No |
| R5 | The in-memory `checkRateLimit` is per-process, not distributed | F15_SECURITY_HARDENING_REPORT.md/F15_PERFORMANCE_BASELINE.md, pre-existing (unchanged), newly extended to 3 more routes | Low-Medium | Certain (architectural fact) | On a multi-instance deployment, the effective limit is `limit × instance count`, weaker than intended | Acceptable at small pilot scale; a Redis-backed limiter is the real fix for anything larger | Rate limiting / infra | No | Yes |
| R6 | No backup/restore automation or documented RPO/RTO exists anywhere in this codebase | F15_BACKUP_RECOVERY_MODEL.md — grep-confirmed absent | Medium-High | Certain (confirmed) | An operator has no rehearsed recovery path if the pilot database is corrupted/lost | Operator must identify the real DB provider's own backup offering and rehearse a restore | Database operations | Recommended before Pilot, not a hard code-level gate | Yes |
| R7 | No paging/alerting/incident-communication tooling exists | F15_SUPPORT_AND_INCIDENT_MODEL.md | Medium | Certain (confirmed) | A Sev 1/2 incident may go undetected until a user reports it | Acceptable for a small, closely-operator-watched pilot; a real gap beyond that | Operations | No | Yes |
| R8 | No dedicated feature-flag/kill-switch beyond the existing `AI_ENABLED` | F15_ROLLBACK_AND_CONTAINMENT_MODEL.md | Low-Medium | Certain (confirmed) | Cannot cleanly disable just Exam Prep/Teacher assignments/Institution Intelligence without a code deploy | `AI_ENABLED=false` provides partial containment for AI-generation-dependent failures specifically | Operations | No | Yes, for anything beyond a small pilot |
| R9 | No `/api/health` endpoint exists | F15_TARGET_PILOT_ARCHITECTURE.md | Low | Certain (confirmed) | No cheap, standard uptime-monitoring target | Real, low-risk, low-effort future addition | Operations | No | Yes |
| R10 (carried, unworsened) | Institution Coverage/Readiness pages still require a raw structure/exam-version ID (no picker) | F14, unchanged | Low-Medium | Certain | Not usable by a non-technical institution admin as-is | Disclosed; mirrors F13's own Teacher-form precedent | Institution Intelligence | Yes, for real institution-admin usability | No |
| R11 (carried, unworsened) | Full item-by-item exam UI's question-review-after-grading screen is minimal (no full per-item breakdown UI, though the data is real and available) | F15_EXAM_TAKING_EXPERIENCE.md, disclosed | Low | Certain | Less pedagogically rich than it could be | `getSimulationScoreSummary`'s `byComponent` data is real and ready for a future phase to surface | Exam-Taking UX | No | No |
| R12 | No `aria-live` verification with real assistive technology | F15_ACCESSIBILITY_CERTIFICATION.md | Low | Certain (disclosed) | The fix made this phase is unverified with a real screen reader | Code-level fix is real and follows standard practice; live AT confirmation recommended | Accessibility | No | Yes, for a genuine accessibility certification claim |
| **R13 (NEW, FASE 0 FREEZE, 2026-09-21)** | The seeded Pilot exam catalog (`F15_PILOT_EXAM_CATALOG_SEED_MANIFEST.md`) has exactly one canonical concept (`Linear Equations`); a real `MINI_MOCK` attempt returned only 1 question with `CONCEPT_NOT_MATCHED` and no completable content, only a skip action | Manual live attempt, 2026-09-21; `docs/implementation/f15/F15_PHASE0_ACCEPTANCE_FREEZE.md` | **Critical** | Certain (directly observed) | No real Student can take a usable exam under the current Pilot catalog; the "exam-taking experience" cannot be demonstrated end to end | Not a code defect — the runner/skip-handling behaved exactly as designed (F15_EXAM_TAKING_EXPERIENCE.md's own honest `ITEM_UNAVAILABLE` states). The fix is content/blueprint work: expand the canonical-concept/objective-mapping coverage for the seeded subject (or scope the Pilot to a exam mode/blueprint the current single concept actually supports) before inviting real users | Academic content / assessment blueprint | **YES — hard gate** | Yes |
| **R14 (2026-09-21; RESOLVED — applied to Preview, same day)** | `20260921_1000_f3_subscription_entitlement_foundation.sql` (already applied in Preview) and `20260921_1000_student_initiated_parent_invitation.sql` (never applied) share the identical canonical migration version `20260921_1000` (same date, same sequence) | Discovered via a real dry-run of the governed migration runner's own `findDuplicateFileVersions` check (`src/lib/migration-ledger.ts`), executed against the real Preview ledger through an operator-authorized, since-removed temporary diagnostic route; no write occurred | **High** | Certain (directly observed, reproducible) | `student_initiated_parent_invitation` can never be correctly recognized as pending by `scripts/db-migrate.ts` while both files share this version — it is misreported as checksum drift against the unrelated, already-applied F3 migration. The entire student→parent invitation flow (built in the onboarding/authorization rework phase) remains inert in Preview and cannot be safely activated via the normal governed path until this is resolved | Renamed the never-applied file to `20260921_1100` (next unused sequence, same date; F3 untouched) and applied both it and `admin_user_management` to Preview under separate, explicit operator authorizations — see the annotation below the table | Migration governance | ~~Yes — blocks the parent-invitation flow from ever being appliable via the normal path~~ **No — resolved and applied** | ~~Yes, same reason~~ **No — resolved and applied** |

**[R14 update, 2026-09-21, same day]**: resolved in code, per explicit operator authorization. `20260921_1000_student_initiated_parent_invitation.sql` (never applied under this name — verified against Preview's real ledger before the rename) was renamed to `20260921_1100_student_initiated_parent_invitation.sql` (same date, next unused sequence; F3's own file/identity, already applied, was never touched). Content unchanged except a header comment recording the rename and its justification.

**[R14 update, 2026-09-21, same day — APPLIED]**: per explicit, separate operator authorization, `20260921_1100_student_initiated_parent_invitation.sql` and `20261011_1000_admin_user_management.sql` were both applied to the real Preview database, in that order, via a temporary operator-authorized diagnostic route deployed from an isolated worktree (never containing any Fase 2A functional code) — same pattern as the earlier one-migration trigger. Ledger went from 32 to 34 rows. F3's own row (`20260921_1000`) confirmed intact both by an explicit identity check before applying and by a post-apply idempotent dry-run reporting zero drift. `parent_invitations` table + its unique index, and `admin_audit_log` + `users.is_test`/`user_roles.revoked_at` columns + the widened `users_status_check_v2` constraint, all confirmed present after applying. Protected table counts (`students`/`users`/`profiles`/`institutions`/`institution_memberships`) unchanged before/after; `users.is_test` count and `user_roles` STUDYUS_ADMIN count both `0` after applying (no test accounts created, no admin role granted by the migrations themselves). The temporary route and its token were removed/revoked immediately after; the two temporary deployments used were removed via `vercel remove`; the isolated worktree was deleted. **Status: RESOLVED — applied to Preview.** The admin user-management *functionality* (routes/UI) remains undeployed — only its schema now exists in Preview, ready for the code to be deployed under separate authorization.

## Explicitly NOT a residual risk (proven, not assumed, this phase)

- Zero domain behavior regressed: 16/16 real-Postgres certifications re-run and passing (including two real bugs found and fixed IN the F12 cert script itself, both fully resolved and re-verified).
- Full unit/integration suite: 352/352 files, 5632/5632 tests passing (up from F14's own 350/5614 — 18 new tests this phase, zero regressions).
- Dependency security: 0 vulnerabilities (`npm audit`), down from 1 critical + 1 high at this phase's own start.
- Grading for the new Exam-Taking flow is 100% delegated to the real, unmodified `recordSimulationItemResponse` — never re-implemented (structurally verified by unit test assertion on the exact call arguments).
- MIN_COHORT_POLICY is now a real, versioned, documented product decision — not an invented number (ADR-F15-MIN-COHORT-POLICY.md's own stated rationale).
- No secret was ever exposed, printed, or committed to git by this phase's own actions (verified directly).
- (F15-C1) The real Preview database migration state is now VERIFIED, not assumed: 32/32 migrations applied, 0 pending, 0 checksum drift, identity integrity (students/users/profiles/user_roles/broken-links/duplicates) independently re-derived at 0 anomalies via a temporary, credential-free diagnostic route queried directly by this agent.
- (F15-C1) The Preview Clerk misconfiguration is now VERIFIED FIXED, not assumed: `/sign-in` re-checked live by this agent on the current deployment.
