# F9 — Residual Risk Register

Date: 2026-09-19. Scope: risks remaining after F9 Preview certification. None block Preview; several explicitly gate pilot/Production readiness.

| # | Risk | Severity | Where it lives | Mitigation status |
|---|---|---|---|---|
| R1 | No real AI provider has ever generated or graded a real simulation item — F9's item generation/grading path reuses F7/F8's own AI call sites unmodified, but neither has been exercised with F9's specific simulation-plan-constrained prompting in practice. | High | `src/lib/simulation/scoring.service.ts`, F7's `generation-contract.service.ts` | Registered as `IVG-F9-01`; must run before any pilot use of the simulation-attempt flow |
| R2 | PAA Full Mock is correctly, truthfully `NOT_READY` — Reading (and by extension Writing/English, never even modeled) remains genuinely unsupported. This is not a defect, but it does mean F9's own PAA vertical cannot yet demonstrate a real Full Mock end-to-end. | High (by design) | Real repository configuration, not code | Explicitly out of scope to fake (task §43); the correct next step is content/configuration work (F6/F7-level), not another F9 code change |
| R3 | The minimal simulation-response endpoint accepts an already-shaped `GeneratedQuestion` from the caller rather than generating it server-side — there is no real end-to-end "plan names a target → AI generates a constrained item → learner answers → AI grades it" loop wired into F9 yet. | Medium | `src/app/api/simulation/attempts/[id]/responses/route.ts` | Deliberate task §33-analog scope decision, matching F8's own precedent; a natural next-phase completion |
| R4 | `readiness_snapshots`/`simulation_plans`/`simulation_attempts` have no UI surface yet — only the API exists. | Medium | Entire readiness/simulation module | Explicitly out of scope per the task's own "F10+ owns the experience/dashboard layers" (§69) |
| R5 | The readiness policy's thresholds (minimum diagnosed targets, evidenced-fraction cutoffs, evidence-sufficiency counts) are first-guess defaults, never calibrated against real learner data — same category of risk F8 already carries for its own diagnostic thresholds. | Medium | `database/migrations/20260927_1000_f9_exam_readiness_simulation.sql` seed data | Versioned and safely replaceable via `createReadinessPolicyVersion`; no retrofit of historical snapshots when it changes |
| R6 | `score_conversion_models` is real, tested, working code with zero real rows — `CAN_PROJECT_OFFICIAL_SCORE` will report `NOT_AVAILABLE_NO_CALIBRATION` for every real exam version until a genuine calibration study backs one. | Medium | `src/lib/readiness/score-projection.service.ts` | Correct, honest behavior (task §29); registering a real model is a future content/measurement-science workstream, not an engineering gap |
| R7 | The `SPEED_FLUENCY_READINESS`/`SIMULATION_PERFORMANCE` dimension thresholds (e.g. `averageScorePercent >= 70` for STRONG) are placeholder cutoffs, not derived from any real score distribution. | Medium | `src/lib/readiness/dimension-classification.algorithms.ts` | Same versioned-policy mitigation as R5 |
| R8 | The additive `idempotency_key` on F7's `exam_attempt_item_responses` is optional and NULL for any caller that doesn't supply one — a future F7-only caller (if one is ever added) gets zero protection unless it explicitly opts in. | Low | `src/lib/assessment/evaluation.service.ts` | Matches F5's own `operation_key` precedent exactly (opt-in, not retroactively enforced); documented explicitly in the migration's own header comment |
| R9 | No load/performance testing exists beyond the small local baseline (R-level: this is explicitly out of scope per task §53's own "baseline characterization only, not production capacity" framing). | Low | N/A (deferred by design) | `F9_PERFORMANCE_BASELINE.md` states this limitation explicitly |
| R10 | Remote Preview database migration state was never independently confirmed (carried forward from F7/F8 — `IVG-F7-01`, now also covering F9's own new tables). | Medium | Deployment process | Consistent with F1–F8 precedent; should be confirmed before any pilot use of `/api/readiness/*`/`/api/simulation/*` against real Preview data |

## Explicitly out of scope for F9 (not risks — deliberate non-goals per the task spec)

- Fake official score projection, admission probability, pass probability (task §29/§69 — never implemented, structurally impossible given `PolicyComparisonResult`'s own shape).
- Final Parent/Teacher/Institution Intelligence dashboards, final UX redesign (task §69 — F10+'s job).
- Production capacity certification, Production migration, Production deployment.
