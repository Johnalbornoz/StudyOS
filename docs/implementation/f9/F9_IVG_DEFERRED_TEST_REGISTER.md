# F9 — Integrated Verification Gate (IVG) Deferred Test Register

Reconciles and continues `F8_IVG_DEFERRED_TEST_REGISTER.md` (task §50/51). No prior entry is silently removed — each is either carried forward unchanged, marked resolved with evidence, or explicitly superseded with a reason. New F9-specific deferrals are appended.

## Carried forward from F7/F8

| ID | Phase origin | Requirement | Status | Notes |
|---|---|---|---|---|
| IVG-F7-01 | F7 | Remote Preview database's actual migration state should be confirmed before pilot use against real Preview data | **STILL OPEN** | Unchanged — F9's own migration is also only certified against local ephemeral Postgres, for the same reason (remote isolation from Production not proven in this environment). Applies equally to F9's own new tables now. |
| IVG-F8-01 | F8 | AI_REAL_PROVIDER certification — valid framework-aware teaching output, malformed-output rejection, language adherence, concept alignment, schema compliance | **SUPERSEDED by IVG-F9-01** | F9 introduces no new AI call site (see `F9_AI_PROVIDER_CERTIFICATION.md`) — F8's and F9's real-provider gaps are the same underlying F7/F8 call sites, so they are consolidated into one forward-looking item (`IVG-F9-01`) rather than tracked as two separate, overlapping deferrals. The original requirement text is preserved here, not deleted. |
| IVG-F8-02 | F8 | Remote authenticated E2E matrix (Student/Parent/Teacher/Entitlement) against live Preview | **STILL OPEN, EXTENDED** | Unchanged in substance; now also covers F9's own new routes (`/api/readiness/*`, `/api/simulation/*`), which share the exact same authorization/entitlement primitives F8's routes already used |
| IVG-F8-03 | F8 | Live Preview admin accept-path (F8's diagnostic/intervention-policy admin routes) exercised as authenticated ADMIN requests | **STILL OPEN, EXTENDED** | Unchanged in substance; now also covers F9's own admin routes (`/api/admin/readiness/policy`, `/api/admin/readiness/score-conversion-models`) |

## New in F9

| ID | Requirement | Why deferred | Dependency | Risk if not executed | Planned IVG execution | Pass criteria |
|---|---|---|---|---|---|---|
| IVG-F9-01 | AI_REAL_PROVIDER certification (task §48) — framework-correct generated item, blueprint-constrained generated item, invalid output rejection, language adherence, evaluation response | No provider credentials available in this environment (confirmed directly, case W of the adversarial matrix) | Preview/non-production AI provider credentials | F7/F8's existing `resolveGenerationContext`/`validateItemForExamContext`/`gradeAnswer` call sites (which F9 reuses unmodified for simulation item generation/grading) are proven only against fabricated payloads and pure logic, never a real model's actual output | At IVG time, run a small controlled batch of real simulation-item generation + grading calls against Preview/non-production credentials, recording provider/model/latency/validation result per task §48's required fields | Every valid case passes F7's existing validation gate; a real provider timeout/4xx/5xx is correctly classified via the existing `AIErrorCode` taxonomy |
| IVG-F9-02 | REMOTE_MIGRATION (task §57) — confirm F9's own migration (readiness/simulation tables + the additive `idempotency_key` column on F7's `exam_attempt_item_responses`) applies cleanly to the real Preview database | Remote migration policy: never apply to remote Preview DB until isolation from Production is proven (unchanged standing policy, task §57) | A verified-isolated staging/Preview database, or an explicit isolation proof | The real Preview database's actual schema state relative to F9's migration has never been independently confirmed | At IVG time, once isolation is proven, apply F9's migration to the real Preview database and run the same schema-verification queries the local cert script runs | All expected tables/columns/constraints present; idempotent re-application produces no error |
| IVG-F9-03 | Full breadth of `evaluation.service.ts`'s new idempotency conflict-recovery path under REAL network-level retry conditions (not just in-process `Promise.all`) | This certification proved the database-level unique-index race is handled correctly under true in-process concurrency; a real HTTP client retry (e.g. a mobile app's exponential backoff after a timeout) exercises the same code path but was not simulated at the network layer | A real or scripted HTTP client capable of simulating a slow/dropped response followed by a retry | Low — the database-level protection is real and proven; this would only add confidence at the transport layer, which is a thinner risk surface | At IVG time, script an HTTP-level retry against a real (Preview) `/api/simulation/attempts/[id]/responses` call | The retried request returns the same `responseId` and `duplicate: true`, matching the in-process proof exactly |

## Discipline

No entry above is silently upgraded to PASS anywhere in this phase's reports. `F9_QA_REPORT.md`'s test-layer matrix marks `AI_REAL_PROVIDER` and the `REMOTE_AUTHENTICATED_E2E`/`REMOTE_MIGRATION` portions as `DEFERRED`, never `PASS`, per task §49/§57's explicit requirement. IVG-F8-01's supersession is recorded, not deleted — a future reader can trace exactly why it no longer appears as its own open item.
