# F8 — Integrated Verification Gate (IVG) Deferred Test Register

This document formally begins the IVG register (task §52). Every deferred cross-phase operational test — from F8 or carried forward from an earlier phase's own reports — is listed here with an explicit ID, so no deferral is ever silent.

## Carried forward from F0-S through F7

| ID | Phase origin | Requirement | Why deferred | Dependency | Risk if not executed | Planned IVG execution | Pass criteria |
|---|---|---|---|---|---|---|---|
| IVG-F7-01 | F7 (`F7_RESIDUAL_RISK_REGISTER.md` R9) | Remote Preview database's actual migration state should be confirmed before pilot use of `/api/admin/assessment/*` against real Preview data | F7's real-Postgres certification used ephemeral local Postgres only (remote isolation from Production could not be proven from this environment, per task's own standing migration policy) | A verified-isolated staging/Preview database, or an explicit isolation proof | Preview API routes could be exercised against a database whose actual applied-migration state was never independently confirmed | At IVG time, connect to the real Preview database (once isolation is proven) and run the same schema-verification queries the local cert script runs | All expected tables/columns/constraints present; F7 and F8 migrations both applied cleanly |

No other F0-S–F6 report documented a deferred *test* (as opposed to a deferred *feature*) in a form this register can honestly carry forward without inventing one — this is deliberate, per task §52's explicit prohibition on inventing prior deferred conditions.

## New in F8

| ID | Requirement | Why deferred | Dependency | Risk if not executed | Planned IVG execution | Pass criteria |
|---|---|---|---|---|---|---|
| IVG-F8-01 | AI_REAL_PROVIDER certification (task §51) — valid framework-aware teaching output, malformed-output rejection, language adherence, concept alignment, schema compliance, against a real provider | No `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` (or any provider credential) is configured in this environment — confirmed by direct check, not assumed | Preview/non-production AI provider credentials | The AI teaching-generation contract's two-pass validator (`checkTeachingContentDeterministic` + `verifyTeachingContentSemantic`) is proven only against fabricated payloads and pure logic, never against a real model's actual output shape/failure modes | At IVG time, run a small controlled batch of real `generateTeachingContent` calls (one per intervention type, one deliberately malformed-input case) against Preview/non-production credentials; record provider/model, latency, validation result per task §51's required fields | Every valid case passes both validation passes; a real provider timeout/4xx/5xx is correctly classified via the existing `AIErrorCode` taxonomy; no invented content is ever served |
| IVG-F8-02 | REMOTE_AUTHENTICATED_E2E — the parent/teacher/entitlement-state authenticated matrix from task §50 (Parent accepted/revoked, Teacher active/wrong-class, Entitlement ACTIVE/SUSPENDED), executed against a real Preview deployment with real authenticated sessions | No mechanism in this environment to authenticate as a real Clerk user against the live Preview deployment; building one is out of proportion to F8's own scope (F2/F3's own real-Postgres certifications already proved this exact matrix for their own routes during F2/F3's respective phases) | A real or scripted Clerk session against the Preview deployment | An F8 route's `canAccessLearner`/`canUseCapability` wiring is proven correct locally (real Postgres, real functions, task §33 case Q) and at the unit/mock level (`f8-api-routes-security.test.ts`), but never end-to-end through a real HTTP request against the deployed Preview app | At IVG time, script an authenticated session (or use a Preview-safe test account) and exercise: Student A -> own resource ALLOW, Student A -> Student B DENY, Parent accepted -> ALLOW, Parent revoked -> DENY, Teacher active assignment -> ALLOW, Teacher wrong class -> DENY, Entitlement ACTIVE -> ALLOW, Entitlement SUSPENDED -> DENY, all against the real deployed routes | Every case matches the already-proven local behavior exactly |
| IVG-F8-03 | Full breadth of the diagnostic/intervention policy versioning admin routes (`/api/admin/diagnostics/policy`, `/api/admin/teaching/intervention-policy`, `/api/admin/teaching/command-term-interpretations*`) exercised as authenticated ADMIN requests against the real Preview deployment, not just anonymous/non-admin denial | No admin Clerk session available in this environment to exercise the accept path (only the anonymous-401/non-admin-403 denial paths are smoke-tested against live Preview, matching F7's own precedent) | A real admin-allowlisted Clerk session against Preview | The admin accept-path (successful policy version creation) is proven locally (real Postgres, via the seed/cert scripts calling the underlying services directly) but never through the actual HTTP admin route against deployed Preview | At IVG time, exercise each admin route's POST with a real admin session against Preview | Each route creates the expected versioned row, matching local behavior |

## Discipline

No entry above is silently upgraded to PASS anywhere in this phase's reports. `F8_QA_REPORT.md`'s test-layer matrix marks `AI_REAL_PROVIDER` and the `REMOTE_AUTHENTICATED_E2E` portion of `AUTHENTICATED_E2E` as `DEFERRED`, never `PASS`, per task §49/§51's explicit requirement.
