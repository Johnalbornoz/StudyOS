# F11-C4 — Residual Risk Register

| # | Risk | Severity | Mitigated by | Residual exposure |
|---|---|---|---|---|
| 1 | A genuine FAILURE (not a race) between `startSimulationAttempt` succeeding and the enclosing transaction committing can still leave a real, unreferenced `exam_attempts`/`simulation_attempts` orphan | Low | The concurrency RACE for this exact window is structurally eliminated (see Concurrency Report); only a true mid-flight exception (e.g. a DB connectivity blip) in that narrow window can produce this, and no such orphan carries any Evidence, readiness, or diagnostic effect since nothing was ever submitted against it | No generic "list my simulation attempts" endpoint exists in F9 today (confirmed by inspection), so such an orphan is never surfaced to the Student; a future phase adding one should re-examine this residual |
| 2 | Only TOPIC_EXAM/DOMAIN_EXAM/MINI_MOCK/FULL_MOCK are supported at the schema level, but a Teacher can assign any of the four regardless of whether the underlying exam version is actually ready for it | Low | This is deliberate, not an oversight: real F9 eligibility is the ONLY gate, checked unconditionally, so an unready assignment is safely rejected at start time rather than being pre-filtered at assignment time | A future phase could add an eligibility PRE-check at assignment time for earlier Teacher feedback (a UX improvement, not a safety gap) -- not built now, mirroring F11-C2/C3's own precedent of deferring assignment-time status pre-checks to start-time enforcement |
| 3 | `academic_subject_id` (new, DOMAIN_EXAM-only column) is not validated against the exam version's own blueprint at ASSIGNMENT time -- an assignment could name a subject with no blueprint targets at all | Low | Caught at START time by `getSimulationEligibility` (`NO_BLUEPRINT_TARGETS_FOR_SUBJECT` / `NO_SUPPORTED_TARGETS_FOR_SUBJECT`), exactly like `learning_objective_id`'s own equivalent case | Same UX-only gap as risk #2; not a correctness or security issue |
| 4 | Exam Reinforcement always runs `timingMode: 'UNTIMED'`, a deliberate, disclosed choice (never `OFFICIAL_SIMULATION_TIMED`) | None -- deliberate | Teacher-assigned reinforcement is explicitly practice, not an official timed mock; using UNTIMED also structurally avoids `TimingConfigurationError` for any component lacking timing configuration, which is appropriate for a reinforcement activity | If a future phase wants Teacher-assigned TIMED reinforcement, this should be a new, explicit, disclosed decision -- not a silent default change |
| 5 | Simulation responses in the current, real F9 implementation never attach `competencyIds` metadata (only `skillIds`) | None -- a real, current-state fact | Documented explicitly in the assessment doc rather than assumed; F11-C4 introduces no code that would add this, so it neither creates nor fixes this gap | If F9 is later extended to also tag Competency evidence from simulation responses, F11-C4 needs no change -- it merely observes whatever metadata the real writer produces |
| 6 | The recurring positional-index test-breakage pattern (`storeQuiz`'s trailing parameters, flagged in F11-C2/C3) does not apply to this phase's own schema change (`assignTeacherIntervention`'s INSERT uses named, not positional-from-the-end, column additions) -- but F11-C4 did not verify whether a FUTURE phase touching `storeQuiz` again would need the same proactive check | Low | Out of scope for this phase's own changes; still flagged here for continuity, since F11-C4 did not add a `storeQuiz` parameter itself | A future phase (Exam-tagged Practice quizzes, if ever built) should re-apply the proactive-check discipline established in F11-C3's own QA report |

## Explicitly NOT a residual risk (proven, not assumed, in this run)

- PAA Full Mock cannot be bypassed by Teacher assignment: proven with the real, current, honestly-NOT_READY fixture, not a hypothetical.
- Framework/version isolation (PAA vs Cambridge): proven with a real shared canonical concept and two genuinely separate exam executions.
- Attempt-creation race safety: proven with a real `Promise.all` concurrent call pair and an explicit before/after row count, not assumed from the locking design alone.
- Zero Evidence/readiness/diagnostic/Canonical fabrication: proven with real before/after counts at every checkpoint, not asserted by source guard alone.
- Cross-role authorization (Student/Parent/Teacher/multi-role): proven identically to every prior F11 sub-phase's own already-certified boundary, with no new gap introduced for Exam specifically.
