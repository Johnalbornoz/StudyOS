# F11-C3 — Residual Risk Register

| # | Risk | Severity | Mitigated by | Residual exposure |
|---|---|---|---|---|
| 1 | C1's documented orphan-`quiz_sessions` race residual now also applies to Competency executions | Low (carried forward, not worsened) | Re-proven harmless under the Competency path in this run's real concurrency case (§ Evidence Reconciliation, Case V) | Same as F11-C1/C2: an unreferenced orphan is ordinary, unattributed practice data — never returned, never submitted, never contributing evidence |
| 2 | `resolveDeterministicConceptForCompetency`'s "exactly one candidate" rule means a Competency with genuinely multiple valid concept matches for a student is simply never executable via F11-C3 | Low-medium | Explicit, controlled failure (`StudentInterventionNotStartableError`), never a silent/arbitrary choice — matches F11-C2's own precedent and the task's explicit preference for fail-controlled over guessing | Same open question F11-C2 already flagged: a future phase could add a Teacher-supplied concept-context override if this proves too restrictive; not built now |
| 3 | `competencies.status='ACTIVE'` is checked only at F11-C3's execution-start time, not at F11-B's assignment time | Low | Documented, deliberate choice to avoid touching frozen F11-B code, mirroring F11-C2's identical choice for Skill | A future phase could add the same check to F11-B's `assignTeacherIntervention` if F11-B is ever revisited for its own reasons |
| 4 | Practice is sufficient for Competency **only under F5's current policy** (no context-diversity/multi-concept dimension exists today) | Low-medium | Verified by direct code inspection (grep for `context_id`/`contexts\b` across the entire learner-state module returns zero matches), documented explicitly rather than assumed, in `F11_C3_CURRENT_COMPETENCY_EXECUTION_ASSESSMENT.md` | If F5's Competency aggregation policy is ever extended with a context-diversity, multi-concept, or transfer-context dimension, F11-C3's one-concept-per-execution Practice adapter would under-serve the new contract and must be re-evaluated — this is a genuine, disclosed forward dependency, not a defect in the current implementation |
| 5 | The recurring positional-index test-breakage pattern (task-flagged from F11-C2) was anticipated and fixed proactively this time, but will very likely recur for F11-C4 (Exam) or any future phase adding another trailing `storeQuiz` parameter | Low | Proactively checked and fixed in this phase (§ QA Report); explicitly documented as a recurring pattern to check again next time | No fix eliminates the recurrence — it is a structural consequence of positional-index assertions on a growing parameter list; a future refactor to a named-options object for `storeQuiz` would remove the pattern permanently, but was judged out of scope for an additive phase |
| 6 | `targetSkillIds` and `targetCompetencyIds` are two independent, separately-nullable columns on the same `quiz_sessions` row rather than a single generalized "target tags" structure | None currently — deliberate | Both F11-C2's handoff doc and this phase's own inspection considered a generalized `targetTags: {skillIds?, competencyIds?}` shape and deliberately deferred it, since no current caller needs both simultaneously and a premature generalization was judged worse than two clear, separate, independently-nullable columns | If F11-C4 (Exam) or a later phase needs to tag a single quiz with more than one dimension simultaneously, this decision should be revisited deliberately rather than adding a third parallel column by default |

## Explicitly NOT a residual risk (proven, not assumed, in this run)

- Concept→Competency graph non-inference: proven with a real graph edge and a real Concept-only execution (Case D).
- Skill→Competency graph non-inference ("one of C3's highest-priority assertions"): proven with a real graph edge, a real legitimate Skill execution producing genuine Skill Evidence, and confirmed zero Competency State change (Case E).
- Reverse direction (Competency→Skill fabrication): proven with real Competency executions producing zero Skill Evidence for a mapped Skill (Case T).
- Competency Evidence traceability to explicit Teacher intent: proven via the full metadata chain, not asserted by design alone.
- Assisted-evidence honesty: proven that assisted performance is recorded honestly and correctly excluded from `independentEvidenceCount` by F5's own real projector (Case L).
- Insufficient-evidence-before-threshold: proven that one (or `minimumEvidenceCount - 1`) successful activities are never interpreted as mastery (Case I).
- Cross-role authorization (Student/Parent/Teacher/multi-role): proven identically to F11-A/F11-B/F11-C1/F11-C2's own already-certified boundary, with no new gap introduced for Competency specifically.
