# F11 — Integrated Residual Risk Register

Carries forward each sub-phase's own residual risk register by reference (F11_C1 has none recorded separately; F11_C2/C3/C4 each have their own, unchanged by this gate) and adds only NEW risks visible at the integrated level.

| # | Risk | Severity | Mitigated by | Residual exposure |
|---|---|---|---|---|
| 1 | A Skill and a Competency that share an underlying canonical concept could, in principle, resolve to the SAME per-student concept context if a student's `concept_catalog_mapping` ever legitimately matches two different canonical concepts to overlapping learner concepts | Low | This gate's own fixture-authoring mistake (see QA Report) demonstrated the FAILURE MODE this would cause (ambiguous resolution) is caught structurally by `resolveDeterministicConceptForSkill`/`resolveDeterministicConceptForCompetency`'s "exactly one candidate" rule at the RESOLUTION level — the bug was in the gate's test fixture, not in production resolution logic, which already fails controlled rather than silently collapsing two contexts | If F6/F4 mapping data ever legitimately produces this overlap for a real student, both interventions would independently and correctly reach `AMBIGUOUS`/fail-controlled rather than corrupt each other's Evidence -- not a live risk, but worth keeping in mind for future F4/F6 data-quality work |
| 2 | The integrated gate exercises exactly one Teacher/class/student combination per section; it does not exhaustively cross every authorization case against every intervention type (e.g., "wrong-class Teacher attempting an EXAM intervention specifically" was not separately re-run, only proven once generically via CONCEPT) | Low | Each sub-phase's own certification (F11-C1/C2/C3/C4) already proved the FULL authorization matrix independently, per type; this gate's job was to prove the matrix holds when types are not run in isolation, which it does via `isOwner`/`canAccessClass` being the SAME primitive for all four (a structural, not per-case, guarantee) | If a future phase introduces a type-specific authorization branch (breaking the "one primitive for all four" structural guarantee), this residual would need re-examination |
| 3 | No new migration was introduced by this gate (all schema is F11-C1 through F11-C4's own, already-certified, already-idempotent) | None | N/A | N/A -- recorded for completeness per the deliverable list's own migration-adjacent framing, not because a real gap exists |
| 4 | The pre-existing `F11_INTEGRATED_CERTIFICATION.md` (committed before this gate's own work began) recorded an incomplete `next build` in an earlier sandbox run | None -- resolved | A fresh `next build` in this gate's own worktree completed cleanly with no incomplete-artifact issue | If this recurs in a DIFFERENT environment (CI/Preview), it should be treated as an environment/tooling issue to investigate there specifically, not as evidence of a source defect -- the same source tree that reportedly failed to complete previously now builds cleanly here |

## Explicitly NOT a residual risk (proven, not assumed, in this gate)

- F9 Student self-service is fully independent of F11: proven with real self-service attempts producing zero `teacher_intervention_executions` dependency.
- PAA Full Mock convergence: proven that Teacher assignment and Student self-service hit the identical `getSimulationEligibility`/`getFullMockEligibility` authority, not two parallel implementations that happen to agree today.
- Joint Evidence non-contamination across all four types run together: proven with a real shared-mapping scenario (Skill and Competency both targeting concepts derived from the same canonical subject), not merely asserted from each type's own isolated certification.
- Exam concurrency's stronger guarantee holds even when exercised alongside the other three types in the same session: proven with a real concurrent race in the SAME integrated run, not re-derived from F11-C4's isolated certification alone.
