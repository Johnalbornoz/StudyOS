# F14 — Residual Risk Register

| ID | Description | Severity | Probability | Impact | Mitigation | Owner/domain | Blocking F15? | Blocking Pilot? | Blocking Production? |
|---|---|---|---|---|---|---|---|---|---|
| R1 | No full item-by-item simulation-attempt exam-taking UI exists (`IVG-F14-01`) | Medium | High (definitely missing) | A Student cannot yet take a real, scored Full/Mini Mock/Topic/Domain exam through the new Exam Prep UI end to end — attempt lifecycle (start/pause/resume/abandon) is real, but the actual question-answering surface is not built | Explicitly scoped out, documented, registered as an IVG item rather than shipped half-built or fabricated | Exam Prep / F9-F15 boundary | No (F14's own scope was Experience Completion, not this specific surface) | **Yes** — a pilot cannot rely on self-service exam-taking without this | Yes |
| R2 | Institution Coverage/Readiness pages require a raw structure/exam-version ID typed into a form; no picker exists (`IVG-F14-02`) | Low-Medium | High (confirmed, no backend list) | Real institution admins would need out-of-band knowledge of a UUID to use these two pages | Disclosed; mirrors F13's own precedent for Teacher Exam-target IDs | Institution Intelligence / F12-F15 boundary | No | Yes — not usable by a real non-technical admin as-is | Yes |
| R3 | Institution Teachers page shows a raw `userId` (no display name join in the backend) | Low | High (confirmed, real backend shape) | Cosmetic only; institution admin sees a UUID instead of a teacher's name | Disclosed; a real backend gap (no name field in `InstitutionTeacherSummary`), not fabricated in the UI | Institution Intelligence | No | Yes for pilot polish | No |
| R4 | `parent.service.ts`'s `ChildOverview.examReadiness` field still exists in the backend (unused by any frontend after this phase) | Low | Certain | None currently — it is dead weight, not a live risk, since zero UI renders it | Documented; verified zero frontend consumers via grep; not removed to avoid unnecessary risk to `getChildOverview`'s other real consumers (Admin page, `/api/learners/[id]/summary`) | Parent / F10 | No | No | No |
| R5 | A real, credential-bearing `.env.local` (Clerk/DB/AI-provider secrets) sits in a sibling scratchpad worktree (`f0s-security`) from earlier session work, never cleaned up (`IVG-F14-06`) | Medium | Certain (confirmed present) | If those credentials are still live, any future session/tooling on this same host that happens to resolve into that directory could connect to a real external system | Never committed to git (verified); this phase took no authenticated action; documented for the human operator to rotate/delete | Local dev environment hygiene, not this codebase | No | Yes — should be resolved before broader team/CI access to this host | No |
| R6 | No live authenticated visual/accessibility/performance verification was performed for any F14 surface | Medium | Certain (disclosed) | Genuine live-environment surprises (rendering, AT behavior, real latency) remain unverified until IVG execution | Local structural/compile-time verification is thorough and real; every gap explicitly registered, never silently skipped or marked PASS | All F14 surfaces | No | Yes | Yes |
| R7 | The Assignments nav badge count is approximate (no reconciliation applied to the badge query, unlike the real Assignments page) | Low | Certain (disclosed, deliberate) | A Student could see a nav badge count that's off by one until they open the Assignments page itself, which always reconciles | Deliberate: avoids running a write-side-effect (`reconcileCompletionsForStudent`) on every single page load | Assignments / F11-C1 | No | No | No |
| R8 | `PracticeRunner` supports fewer question-type affordances than the full self-service `/dashboard/quiz` page (no hints, no teaching-stage choreography) | Low | Certain (disclosed, deliberate) | A Teacher-assigned reinforcement quiz feels slightly plainer than a self-service one; grading/scoring is identical and fully real either way | Deliberate scope reduction to avoid touching the complex, already-certified self-service page | Assignments / Practice UI | No | No | No |
| R9 (carried, unworsened) | Two parallel identity/role systems coexist (`src/lib/auth.ts`'s legacy `UserRole` vs. F1's real `Role`/`Workspace`) | Low | Unchanged | Unchanged from F13; F14's own new code exclusively uses the real F1/F9/F11/F12 systems | Unchanged | Platform-wide | No | No | No |

## Explicitly NOT a residual risk (proven, not assumed, in this run)

- Zero domain behavior changed anywhere outside the two disclosed bug fixes (missing EXAM target schema — already fixed in F13; the new `TeacherInterventionExamProfileMismatchError` mapping — fixed this phase): 16 real-Postgres certifications re-ran byte-for-byte unchanged, all PASS.
- No new academic/assessment/readiness engine was introduced: every number any new F14 page renders traces to an already-certified F5/F6/F7/F8/F9/F11/F12 function, verified by direct code reading and a structural source guard (`tests/unit/f14-experience-completion-source-guard.test.ts`, 10/10 passing).
- No new authorization/identity/workspace mechanism was introduced anywhere in F14.
- Grading/scoring for the new Assignment Practice runner is 100% delegated to the real, unmodified `/api/quizzes/generate-and-take` submit branch — never re-implemented.
- No secret was ever committed to git history (verified directly, see F14_ENVIRONMENT_PROVENANCE_REPORT.md).
