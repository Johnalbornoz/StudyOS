# F13 — Residual Risk Register

| # | Risk | Severity | Mitigated by | Residual exposure |
|---|---|---|---|---|
| 1 | The Parent page's legacy `assessment_occurrences.exam_readiness` display remains live, unreplaced | Medium | Documented explicitly (`F13_LEGACY_UX_CONTAINMENT.md`), classified `REQUIRES_F14_MIGRATION`, never extended or reused by anything new | Parents continue to see a pre-F9 readiness percentage until F14 builds a real F9-based replacement for that specific card |
| 2 | Student self-service Exam Prep and Teacher-assigned-intervention visibility have no dedicated Student-facing UI yet | Medium | Both are fully real and certified at the backend/API layer (F9, F11) — this is a UI gap, not a safety or correctness gap; documented explicitly with the exact reason (time allocation, not difficulty) | A Student today cannot see "my Teacher assigned me X" or browse Topic/Domain/Mini/Full Mock from their own dashboard — they would need direct API knowledge, which no real user has |
| 3 | Institution Grades/Classes/Teachers/Coverage/Readiness pages not built | Low-medium | All five underlying F12 functions are real, certified, and ready; Coverage/Readiness specifically need a "pick a context" selector that has no backend list to populate yet (a real, separate gap, not this phase's to invent) | An Institution admin cannot browse grade/class/teacher rosters or curriculum coverage/exam readiness from the new UI yet — only Overview/Learners/Interventions/Attention |
| 4 | Two parallel identity/role systems coexist (`src/lib/auth.ts`'s legacy `UserRole` vs. F1's real `Role`/`Workspace`) | Low (unchanged by F13, not worsened) | F13's own new code exclusively uses the real F1 system; the legacy one was not touched, removed, or extended | A future contributor could still accidentally build against the legacy system for a new surface; no structural guard prevents this specifically (the F13 source guard only checks the files THIS phase created) |
| 5 | Interface-language preference is keyed inconsistently (`students.id` for Student, canonical `users.id` for everyone else) | Low | Deliberate, disclosed, zero-risk-to-existing-data choice (see `F13_ROLE_WORKSPACE_MODEL.md`) | A user who is BOTH a Student and, say, a Teacher will have two independently-set language preferences today, which could surprise them (switch workspace, language "changes" because it's actually a different stored preference) — not tested against a real multi-role fixture in this session |
| 6 | No live authenticated visual/accessibility/performance verification was performed | Medium | Local structural/compile-time/regression verification is thorough and real; every gap is explicitly registered in `F13_IVG_DEFERRED_TEST_REGISTER.md`, never silently skipped or falsely marked PASS | Genuine live-environment surprises (real font-rendering, real screen-reader behavior, real network latency) are unverified until IVG execution |
| 7 | The dev server in this environment connected to a real Clerk instance and an unidentified database via a `.env.local` whose existence could not be confirmed by direct file inspection | Low (handled safely) | No authenticated action or write was attempted; the server was stopped immediately after one safe, read-only, unauthenticated screenshot | If a future session in this same environment assumes "no env configured" based on a similar `ls`/`find` check, it should independently re-verify via the dev server's own boot log before concluding credentials are absent |
| 8 | The Teacher Exam-assignment form shows one generic error message for every rejection reason (including PAA Full Mock NOT_READY) rather than F9's own specific `reasonCodes` | Low | The underlying guard is real, unbypassable, and independently certified (F11-C4) — this is a UX clarity gap, not a safety gap | A Teacher attempting to assign an ineligible exam activity sees "could not create the assignment" rather than the specific, honest reason; registered as `IVG-F13-07` |

## Explicitly NOT a residual risk (proven, not assumed, in this run)

- Zero domain behavior changed anywhere: 16 real-Postgres certifications re-ran byte-for-byte unchanged.
- No new academic/assessment/readiness engine was introduced: every number any new page renders traces to an already-certified F5/F6/F7/F8/F9/F11/F12 function, verified by direct code reading and a structural source guard.
- Role/workspace switching cannot widen permissions: it writes exactly one UI-context column (`users.active_workspace`) via F1's own, unchanged, fail-closed `setActiveWorkspace`.
- The one real backend gap found (missing EXAM schema branch) was found BECAUSE this phase built real UI against it, then fixed and guarded — a genuine value of doing the UI consolidation work, not a risk it introduced.
